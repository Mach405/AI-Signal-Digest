__pycache__/
*.pyc
.DS_Store

# Ephemeral run artifacts — regenerated each run; not needed in the repo.
data/seen.sqlite
data/inbox/
data/digests/
data/scored/
data/market/
data/track_record.jsonl
data/daily.log

# Secrets — never commit (Gmail app password lives here).
config/secrets.env
